# What is RAG?

RAG (retrieval-augmented generation) is a technique that enhances a model's generation by retrieving relevant information from external memory sources — such as an internal database, a user's chat history, or the internet — and feeding that retrieved information into the model as context before it generates a response.