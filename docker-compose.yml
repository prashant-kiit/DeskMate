services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: deskmate
      POSTGRES_PASSWORD: deskmate
      POSTGRES_DB: deskmate
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: